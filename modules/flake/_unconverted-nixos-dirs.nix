# Import-tree exclusion boundary.
#
# The list is empty; every modules/ directory is a discovered contributor or an
# underscore-private helper. Re-add an entry only when a genuinely transitional
# plain-module directory appears.
#
# Contract: tests enumerate this list; entries must exist as directories.
[ ]
