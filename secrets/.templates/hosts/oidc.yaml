# secrets/.templates/hosts/oidc.yaml – cross-host OIDC handshake secrets
# Copy this file for each host and replace <host> with the hostname.
# Encrypted to: owner + identity-provider host + consumer host
# Key path: secrets/hosts/<host>/oidc.yaml
# Only client_secret is needed; client_id is the canonical Kanidm oauth2 client name.
termix:
  client_secret: <value>
beszel:
  client_secret: <value>
quantum:
  client_secret: <value>
karakeep:
  client_secret: <value>
paperless:
  client_secret: <value>
