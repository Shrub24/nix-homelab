# secrets/.templates/services/degoog.yaml – degoog instance settings password
# Encrypted to: home-forge (the only host running the search instance)
# Key path: secrets/services/degoog.yaml
settings_password: <value>   # openssl rand -hex 32; unlocks the degoog settings UI
